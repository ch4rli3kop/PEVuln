

void func_1 (int* array, int len, int index){
    
    int value = get_user_input()

    // check index
    if ( index < len ){
        array[index] = value;
    }    
}

if ( a < 10 ) {
    if ( a < 3 )
        ...
} else {
    if ( b > 20 )
        ...
}

void func_csgo(){

    int max_fileNameLength, max_fileNameLength_again
    int fileNameLength
    char buffer[260]

    max_fileNameLength = 259;
    max_fileNameLength_again = 259;
    if (filenameLength <= 258){
        max_fileNameLength_again = fileNameLength;
        max_filenameLength = fileNameLength;
    }
    memcpy(buffer, fileName, max_fileNameLength);
    ...
    memcpy(buffer, fileName2, fileNameLength);

}